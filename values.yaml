replicaCount: 2

image:
  repository:  nikhil5194/webserver #nginx
  tag: latest
  pullPolicy: IfNotPresent

resources:
  requests:
    memory: "64Mi"
    cpu: "250m"
  limits:
    memory: "128Mi"
    cpu: "500m"

service:
  type: NodePort
  port: 80
  targetPort: 80
  nodePort: 30007
